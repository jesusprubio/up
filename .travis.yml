language: rust

rust:
  - stable
  - beta
  - nightly

before_install:
  - cargo install cargo-cmd
  - cargo cmd deps

matrix:
  allow_failures:
    - rust: nightly
  fast_finish: true

script:
  - cargo cmd test
